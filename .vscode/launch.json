{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Launch",
            "type":"lldb",
            "request": "launch",
            "program": "${workspaceFolder}/target/debug/compounder.exe",
            "args": [],
            "cwd": "${workspaceFolder}",
            "preLaunchTask": "Build"
        },
    ]
}